"""Text-to-speech output.

Currently a print stub — replace the body of speak() with ElevenLabs
(or another TTS) when the voice output is ready.
"""


def speak(text: str) -> None:
    """Speak text aloud. Blocks until playback is complete."""
    # TODO: replace with ElevenLabs API call
    print(f"[Robot] {text}")
