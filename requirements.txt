# Core
sounddevice
numpy
webrtcvad
python-dotenv
openai
requests

# STT: faster-whisper (local) tai voice/stt_openai (cloud)
faster-whisper

# TTS
elevenlabs

# Audio
noisereduce

# Vision: opencv-python for USB webcam fallback. Pi Camera Module 2 requires picamera2 (apt, not pip): sudo apt install python3-picamera2
opencv-python

# Music
yt-dlp

# Optional: face_recognition (vision, needs cmake/dlib)
# pip install face_recognition

setuptools